# Archived n8n workflows

These workflows are intentionally excluded from the production import pack. They are retained only for rollback/history and should not be reactivated without a dependency review.

The production root contains active workflows plus a single deliberately gated executor awaiting manual activation: the Team Task Email executor (`VOSTEAMEMAIL001`); Calendar and Funding Package run active.
